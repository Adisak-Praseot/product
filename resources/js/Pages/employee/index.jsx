export default function Index({ employees}) {
    return (
        <div>
            <h1>Employee List</h1>
                {employees.map((employee, index) => (
                    
            ))}
        </div>
            
    );
}